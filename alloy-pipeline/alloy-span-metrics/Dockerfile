FROM grafana/alloy:latest

# Copy the local config into the default location expected by the Alloy image
COPY config.alloy /etc/alloy/config.alloy

# The base image already has the correct ENTRYPOINT and CMD to run this file
